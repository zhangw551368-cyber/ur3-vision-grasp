# 归档验证结果

验证日期：2026-08-31。

- 全部新增 Bash 入口已通过 `bash -n` 静态语法检查。
- 手眼标定及归档 launch 已通过 XML 解析检查。
- 原始代码快照内的 Python 文件已通过 AST 静态语法解析。
- `ur_robot_driver`、`ur3_dual_moveit_config`、`realsense2_camera`、`tf2_ros`、`robotiq_2f_gripper_control`、`dual_arm_tasks` 和 `rviz` 均可由当前 ROS 环境找到。
- 当前 RealSense launch 确认包含 `align_depth` 和 `enable_sync` 参数。
- 机器人、标定、配置、GraspNet Python 环境和夹爪串口路径在归档时均存在。
- GraspNet 核心测试结果：`14 passed`。
- 归档不包含历史 `.pkl` 轨迹缓存，没有启动 ROS 节点，也没有发送真机运动命令。

这些检查只证明文件和当前软件环境一致，不替代现场硬件状态、目标位置、RViz 轨迹和安全检查。
