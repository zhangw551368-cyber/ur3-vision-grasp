# 终端 09A：HSV 规划、复核与真机执行

只在路线 A 使用，严格按以下顺序：

1. `bash 01_只规划.sh`：不会发送轨迹。
2. `bash 02_复核历史锁点.sh`：30 帧只读比较，不改锁点、不运动。
3. `bash 03_检查真机状态.sh`：检查 External Control、机器人和夹爪。
4. 人工检查 RViz 和现场后，才运行 `bash 04_真机执行.sh`。

历史锁点文件只对应 2026-08-23 的物体与棋盘摆放。若复核失败或场景被移动，不得执行，需要先生成当前场景的新锁点文件。

