# 终端 09B：多样工件分类与像素冻结

只在路线 B 使用。运行 `bash 运行.sh`。节点发布：

```text
/ur3_graspnet6d/object_detection_image
/ur3_graspnet6d/detected_objects_json
```

分类名称可能因金属反光跳变，所以执行任务绑定的是当前原始 1280×720 图像中的物理中心像素和半径，不是持续变化的编号。

