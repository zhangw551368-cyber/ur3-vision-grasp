# 终端 05：新手眼标定 TF

两条路线均可运行 `bash 运行_仅手眼TF.sh`。路线 A 若还需要旧 HSV 相机点转 base 点的话题转换节点，可改运行 `bash 运行_HSV兼容入口.sh`。二者包含同一个 safe 标定文件，只能二选一。

启动后运行 `bash 检查TF.sh`。变换应连续可查；固定相机不应随右臂运动而变化。

禁止同时启动原始旧 launch、其他静态 base-to-camera TF 或标定 GUI 的相机位姿发布器。

